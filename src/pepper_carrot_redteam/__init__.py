"""pepper-carrot-redteam — an agentic red-teamer for the Pepper & Carrot companion.

Discovery half of evaluation: an MCP client that *decides its own probes* to find failures
the deterministic `pepper-carrot-eval` harness never tested for, then writes confirmed
failures back as candidate gold. See docs/DESIGN.md and docs/decisions/0001-*.md.
"""

__version__ = "0.1.0"
